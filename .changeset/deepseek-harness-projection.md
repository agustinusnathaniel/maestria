---
'@maestria/deepseek': minor
'maestria': minor
---

Add a DeepSeek Harness projection: 14 generated Agent-Skills, a Cordis plugin contributing the routing/global-rules prompt sections, specialist persona variables, and a skills provider, plus a self-contained Maestria agent preset with per-specialist subagent delegation tools. Registers the `deepseek` platform in the CLI (`install`/`update`/`uninstall` stage the preset into `$DSH_HOME/.agent-presets/maestria`).
