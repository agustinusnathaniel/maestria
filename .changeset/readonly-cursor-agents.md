---
'@maestria/cursor': patch
---

Add `readonly: true` runtime flag to read-only agents (adventurer, planner, reviewer), upgrading maker/checker enforcement from prompt-level-only to two-layer defense
