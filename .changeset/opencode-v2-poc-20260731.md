---
'@maestria/opencode-v2': minor
---

feat: add opencode-v2 POC package for V2 plugin API

New experimental package that ports Maestria's methodology to the OpenCode V2 beta plugin API using Plugin.define() pattern, ctx.agent.transform() for agent registration, and ctx.session.hook() for mode detection and rules injection.
