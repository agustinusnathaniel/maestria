---
"@maestria/pi": patch
"@maestria/omp": patch
---

Clarify delegation terminology as contract-driven delegation (historical alias Spec-Driven Delegation): spec-aware but format-agnostic handoffs with optional persistent intent refs via the spec-contract skill. Docs clarification only, no behavior change.
