---
"maestria": patch
---

Tidy `maestria doctor` human output without changing JSON or behavior. Observed lists show managed skills only with an omitted count pointing to `--json` for the full list, platforms sharing one agent inventory render it once with later platforms referencing the first, and Next shows a single actionable step (install first, then record, then the first remaining step).
