---
title: '@maestria/cursor'
description: Declarative Cursor plugin that ships specialist agents, an orchestrator skill, global rules, and workflow commands for Cursor IDE and Cursor CLI.
template: splash
editUrl: false
lastUpdated: false
---

import CapabilityLedger from '@/components/plugin/capability-ledger.astro';
import DocsLinkList from '@/components/plugin/docs-link-list.astro';
import PlatformOverview from '@/components/plugin/platform-overview.astro';
import PluginSection from '@/components/plugin/plugin-section.astro';
import SurfaceComparison from '@/components/plugin/surface-comparison.astro';

<PlatformOverview
  platformId="cursor"
  heroTitle="One bundle, two surfaces."
  heroLead="Install one plugin for 7 specialist agents, an orchestrator skill, always-on rules, and fein, sonar, and blitz commands across Cursor IDE and CLI."
  facts={[
    {
      label: 'Shared surface',
      value: 'Cursor IDE and CLI discover the same local plugin bundle.',
    },
    {
      label: 'Enforcement',
      value: 'Read-only roles use readonly: true where supported; all other prompt guidance is advisory.',
    },
  ]}
  primaryCta={{ href: '/cursor/getting-started/installation/', label: 'Get started' }}
  secondaryCta={{ href: '/cursor/getting-started/quick-start/', label: 'Quick start' }}
  installCaption="Installs the local plugin to ~/.cursor/plugins/local/maestria for both Cursor surfaces."
>
  <SurfaceComparison
    description="The same local plugin is discovered by two Cursor surfaces. The bundle is shared, the entry point is not."
    left={{
      details: [
        'Cursor IDE loads the plugin from ~/.cursor/plugins/local/maestria.',
        'Use Customize -> Plugins, then restart or reload the window.',
        'Agent chat exposes the seven Task agents and workflow commands.',
      ],
      href: '/cursor/getting-started/installation/',
      mark: 'cursorMark',
      name: 'Cursor IDE',
      summary: 'The graphical Agent surface where the local plugin is discovered and specialists are selected.',
    }}
    note="IDE and CLI share the plugin contract, but discovery and session surfaces remain Cursor-owned."
    right={{
      details: [
        'Cursor CLI uses agent; cursor-agent remains a compatibility alias.',
        'It discovers the same local plugin directory.',
        'The same specialists, orchestrator, rules, and commands are available from Agent chat.',
      ],
      href: '/cursor/getting-started/quick-start/',
      name: 'Cursor CLI',
      summary: 'The terminal Agent surface for the same bundle, without a separate CLI plugin.',
    }}
    title="One bundle, two Cursor surfaces."
  />

  <PluginSection id="cursor-capabilities" title="What the shared plugin carries.">
    <CapabilityLedger
      ariaLabel="Cursor plugin contract ledger"
      items={[
        {
          detail: 'The .cursor-plugin/plugin.json manifest points to generated rules, agents, skills, and commands.',
          label: 'Distribution shape',
          value: 'One declarative plugin',
        },
        {
          detail: 'Adventurer, architect, builder, diagnose, planner, reviewer, and writer run as Cursor Task agents.',
          label: 'Task agents',
          value: '7 specialist agents',
        },
        {
          detail: 'The orchestrator auto-loads when relevant, or can be invoked through /orchestrator.',
          label: 'Orchestrator',
          value: '1 routing skill',
        },
        {
          detail: 'rules/maestria-global.mdc sets alwaysApply: true for every Agent session.',
          label: 'Global rules',
          value: 'Always-on .mdc rules',
        },
        {
          detail: '/fein runs the full pipeline, /sonar is research-only, and /blitz takes the fast path.',
          label: 'Workflow commands',
          value: 'fein, sonar, blitz',
        },
      ]}
    />

    <nav class="plugin-docs" aria-labelledby="cursor-docs-title">
      <h3 class="plugin-docs__title" id="cursor-docs-title">Documentation</h3>
      <div class="plugin-docs__links">
        <DocsLinkList
          compact
          items={[
            { title: 'Getting Started', href: '/cursor/getting-started/installation/' },
            { title: 'Quick Start', href: '/cursor/getting-started/quick-start/' },
            { title: 'Agent Reference', href: '/core/agents/' },
            { title: 'Workflow Patterns', href: '/core/workflow-patterns/' },
            { title: 'Changelog', href: '/cursor/changelog/' },
            { title: 'Contributing', href: '/cursor/contributing/' },
          ]}
        />
      </div>
    </nav>

  </PluginSection>
</PlatformOverview>
