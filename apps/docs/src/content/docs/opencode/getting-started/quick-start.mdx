---
title: Quick Start
description: Your first session with @maestria/opencode - delegating to specialists and using the orchestrator.
---

import { Aside } from '@astrojs/starlight/components';

Once you've installed the plugin, all agents and rules are loaded automatically in every session - no extra commands, no manual setup.

<Aside type="caution" title="Choose the workflow before choosing the orchestrator">
  Maestria adds coordination and review sessions. For small changes or expensive, slow models,
  direct execution or one specialist is often the better path. Read [When to use
  Maestria](/core/when-to-use/) first.
</Aside>

## Start a Session

Launch OpenCode as usual. The plugin wires itself in. Just start working.

## Delegate to a Specialist

Call any of the 7 specialists directly:

```markdown
@adventurer Explore the auth module and trace the login flow.
```

For unfamiliar, multi-file, or high-risk work, hand it to the orchestrator:

```markdown
@orchestrator I need to add rate limiting to the API. Plan and implement it.
```

The orchestrator breaks the task down, delegates to the right specialists, and integrates the results.

The orchestrator routes work through [thinker → worker → verifier](/core/agents/#orchestration-model)
roles. Use `fein` for full pipeline, `sonar` for research only, or `blitz` for fast implementation.
See the [Agent Reference](/core/agents/) for details.

For longer autonomous runs, the ecosystem also documents an optional pairing:
the independent [OpenCode Goal Plugin](/ecosystem/opencode-goal-plugin/) adds
Codex-style long-running goal mode to OpenCode itself. It complements the
orchestrator's continuation policy; Maestria neither requires nor activates it.

## Next Steps

- [Agent Reference](/core/agents/) - Full docs for all 8 agents
- [Workflow Patterns](/core/workflow-patterns/) - Orchestration and pipeline patterns
