---
title: '@maestria/claude-code'
description: Declarative Claude Code plugin that ships 7 specialist agents, an orchestrator skill, global rules, and workflow commands - no build step, no runtime code.
template: splash
editUrl: false
lastUpdated: false
hero:
  title: 'Engineering Workflows for Claude Code'
  tagline: 'Load one plugin. Get 7 specialist agents, the orchestrator methodology, global rules, and fein/sonar/blitz workflow commands - from a self-contained directory, no install.'
  actions:
    - text: Get Started
      icon: right-arrow
      link: /claude-code/getting-started/installation/
    - text: View Agents
      icon: external
      variant: minimal
      link: /core/agents/
---

import { LinkCard, CardGrid, LinkButton, Aside } from '@astrojs/starlight/components';

`@maestria/claude-code` is a declarative [Claude Code](https://code.claude.com) plugin: a
`.claude-plugin/plugin.json` manifest, a directory of agent files, two skills, and three workflow
commands. The agent files, skills, and workflow commands are generated from the canonical
directives in `packages/core/agent-directives/`; the plugin manifest is maintained separately in
the package.

> **Status: Native candidate.** The plugin validates cleanly with the official CLI
> (`claude plugin validate --strict`) and follows the current documented plugin contract, but
> runtime behavior is **not yet tested end to end**. Upstream Claude Code docs are moving and
> unpinned - reverify before relying on any claim. Do not treat this package as a production
> support promise. See the [runtime support matrix](https://github.com/agustinusnathaniel/maestria/blob/main/docs/runtime-support-matrix.md) for evidence.

## How It Works

1. **Plugin loads** - Claude Code discovers the `.claude-plugin/plugin.json` manifest and the `agents/`, `skills/`, and `commands/` directories from the plugin directory
2. **Rules preloaded** - every specialist agent preloads the `maestria:global-rules` skill via the agent `skills` frontmatter field
3. **Agents available** - 7 specialist agents are available as namespaced `@maestria:<agent>` mentions
4. **Orchestrate** - the `/maestria:orchestrator` skill routes work to specialists; `/maestria:fein`, `/maestria:sonar`, and `/maestria:blitz` select the pipeline depth
5. **Read-only roles enforced** - `adventurer`, `planner`, and `reviewer` deny the `Write` and `Edit` tools via `disallowedTools` (user-authorized; the only runtime enforcement in the package)

## Quick Start

The plugin is a self-contained directory - validate and load it without any install step, from the repository root:

```bash
claude plugin validate ./packages/claude-code --strict
claude --plugin-dir ./packages/claude-code
```

<Aside type="caution" title="Local validation only">
  Marketplace distribution and a CLI install handler (`maestria install claude-code`) are follow-up
  work. Until then, load the plugin from a checkout of this repository with `--plugin-dir`.
</Aside>

<LinkButton href="/claude-code/getting-started/installation/" size="lg">
  Full Installation Guide →
</LinkButton>

## Explore the Docs

<CardGrid>
  <LinkCard
    title="Getting Started"
    description="Local validation, loading the plugin, and your first session."
    href="/claude-code/getting-started/installation/"
  />
  <LinkCard
    title="Agent Reference"
    description="Roles, permissions, and when to use each of the 7 specialists."
    href="/core/agents/"
  />
  <LinkCard
    title="Workflow Patterns"
    description="Practical agent usage patterns - from recon to full build pipelines."
    href="/core/workflow-patterns/"
  />
  <LinkCard
    title="Changelog"
    description="Version history and release notes."
    href="/claude-code/changelog/"
  />
  <LinkCard
    title="Contributing"
    description="Project structure, the sync pipeline, and how to extend the plugin."
    href="/claude-code/contributing/"
  />
</CardGrid>
